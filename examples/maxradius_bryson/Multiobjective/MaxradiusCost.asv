function [Mayer,Lagrange,DMayer,DLagrange]=MaxradiusCost(sol);

t0 = sol.initial.time;
x0 = sol.initial.state;
tf = sol.terminal.time;
xf = sol.terminal.state;
t  = sol.time;
x  = sol.state;
u  = sol.control;
p  = sol.parameter;

Mayer    = -0.5*xf(1)/ + 0.5*tf/3.32; % Maximize the orbital radius
Lagrange = zeros(size(t));
